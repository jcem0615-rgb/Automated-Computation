{
  "name": "logicquot",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first PWA for generating itemized Philippine Peso project quotations.",
  "type": "module",
  "scripts": {
    "start": "http-server . -p 8080 -c-1",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "icons": "node tools/generate-icons.mjs",
    "e2e": "node tools/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
